# StudentLink Frontend Environment
# Copy to .env.local for local development

# REST API base URL (browser calls this — never talks to NFC hardware directly)
NEXT_PUBLIC_API_URL=http://localhost:4000/api

# Public site URL (used for NFC card profile links)
NEXT_PUBLIC_SITE_URL=http://localhost:3000
